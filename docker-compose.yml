services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mobicity-web
    ports:
      - "5173:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
